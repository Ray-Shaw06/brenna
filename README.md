# Surprise Website

Edit `content.js` to change the paragraph, signature, captions, and highlight pills.

Replace the files inside `assets/placeholders` with your real photos, or update the image paths in `content.js`.

Run `start-surprise.bat` to start listening for `I love you`. When the phrase is recognized, the site opens in Chrome fullscreen using the `Default` profile.
